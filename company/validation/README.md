# Validation Evidence

- `templates/`: public-safe result formats
- `results/`: sanitized local results; ignored by default
- `raw/`: internal raw evidence; ignored and retained internally only

Every result must identify the exact candidate commit. Do not copy internal endpoints, actual model IDs, prompts/source, credentials, or raw output into tracked files.
